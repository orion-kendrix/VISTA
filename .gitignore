# Environment
.env
.env.local

# Local clone of the CodeVortex website (separate repo, has its own secrets) —
# must never be committed into this public repo.
website/

# Local design mockups / exploration scratch (not part of the shipped widget).
design-preview/

# Dependencies
node_modules/

# Netlify
.netlify/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
